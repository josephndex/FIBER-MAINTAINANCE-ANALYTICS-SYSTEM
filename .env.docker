# FIBER MAINTENANCE ANALYTICS SYSTEM - Docker Environment
# =========================================================
# This file is for Docker deployment on your Windows PC
# The database runs on your Windows PC, Docker connects to it via host.docker.internal

# Configuration 1: Use this for Docker deployment (connects to host PC database)
DB_NAME_1=fiber_maintainance_department
DB_HOST_1=host.docker.internal
DB_USER_1=root
DB_PASSWORD_1=Fireside.Africa1or!

# Configuration 2: Same as above for Docker (both point to host database)
DB_NAME_2=fiber_maintainance_department
DB_HOST_2=host.docker.internal
DB_USER_2=root
DB_PASSWORD_2=Fireside.Africa1or!
